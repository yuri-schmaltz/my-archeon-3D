[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
addopts = -v --strict-markers
markers =
    unit: Unit tests that run fast and don't require GPU
    integration: Integration tests
    slow: Tests that take a long time
